import { Attribute } from './attribute-directive';

describe('Attribute', () => {
  it('should create an instance', () => {
    const directive = new Attribute();
    expect(directive).toBeTruthy();
  });
});
